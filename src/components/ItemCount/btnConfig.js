import { Button, ButtonGroup } from '@mui/material';
import React from 'react';

export const btnConfig = (
	handleClickRemove,
	handleClickEmpty,
	handleClickAdd,
	handleAddToCart
) => {
	return <></>;
};
